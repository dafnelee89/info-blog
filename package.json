{
  "name": "info-blog",
  "private": true,
  "version": "0.1.0",
  "description": "Chirpy-based Jekyll blog skeleton.",
  "scripts": {
    "dev": "rbenv exec bundle exec jekyll serve --host 127.0.0.1 --port 4101 --force_polling",
    "build": "npm run build:css && rbenv exec bundle exec jekyll build",
    "build:css": "purgecss -c purgecss.config.js",
    "clean": "rbenv exec bundle exec jekyll clean",
    "analyze:ai-content": "node scripts/ai-content-index/index.mjs",
    "verify:indexing": "node scripts/verify-indexing-readiness.mjs"
  },
  "dependencies": {
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "purgecss": "^6.0.0"
  }
}
