# 블로그 포스팅 방법 상세 가이드

이 문서는 `dafnelee89/info-blog` 블로그에서 새 글 초안을 만들고, 초안을 실제 포스팅으로 바꾸고, 로컬에서 확인한 뒤 배포까지 진행하는 절차를 자세히 정리한 문서입니다.

## 전체 흐름

블로그 글 하나를 올리는 전체 순서는 아래와 같습니다.

1. GitHub Actions에서 블로그 주제 후보 생성
2. 마음에 드는 주제를 이슈 댓글로 선택
3. 자동으로 생성된 초안 Pull Request 확인
4. Pull Request 머지
5. VS Code에서 로컬 저장소 동기화
6. `_drafts`의 초안을 `_posts`로 이동
7. 글 내용, 날짜, 이미지 경로, 문장 수정
8. 대표 이미지와 본문 이미지 생성
9. 이미지를 WebP로 변환하고 정해진 경로에 저장
10. 로컬 서버에서 화면 확인
11. 최종 수정 후 GitHub에 푸시

## 1. 블로그 초안 만들기

### 1-1. 주제 후보 생성 워크플로 실행

1. GitHub에서 `blog-automation` 저장소로 이동합니다.
2. 상단 메뉴에서 `Actions`를 클릭합니다.
3. 왼쪽 워크플로 목록에서 `Generate Topic Candidates`를 선택합니다.
4. `Run workflow` 버튼을 누릅니다.
5. 선택 항목에서 `info-blog`를 선택합니다.
6. 다시 `Run workflow`를 눌러 실행합니다.

실행 후에는 워크플로가 끝날 때까지 기다립니다. 보통 주제 후보가 GitHub Issue로 생성됩니다.

### 1-2. 마음에 드는 주제 선택

1. `blog-automation` 저장소의 `Issues` 탭으로 이동합니다.
2. 새로 생성된 주제 후보 이슈를 엽니다.
3. 후보 목록을 읽고 포스팅하고 싶은 주제를 고릅니다.
4. 이슈 댓글에 선택한 번호를 적습니다.

예시는 아래와 같습니다.

```text
pick 2
```

또는 여러 개를 한 번에 선택할 때는 아래처럼 적습니다.

```text
pick 2,3
```

댓글을 남기면 자동화가 선택한 주제를 기준으로 초안 생성을 시작합니다.

### 1-3. 초안 Pull Request 확인

초안 작성이 끝나면 `dafnelee89/info-blog` 저장소에 Pull Request가 생성됩니다.

확인할 내용은 다음과 같습니다.

- Pull Request 제목이 내가 선택한 주제와 맞는지 확인
- 생성된 파일이 `_drafts` 폴더 안에 있는지 확인
- 글 제목, 설명, 태그, 카테고리가 이상하지 않은지 확인
- 자동화가 실패하지 않았는지 Pull Request 화면에서 확인

문제가 없어 보이면 Pull Request를 머지합니다.

### 1-4. Pull Request 머지

1. Pull Request 화면 하단으로 이동합니다.
2. 초록색 `Merge pull request` 버튼을 클릭합니다.
3. 확인 버튼이 한 번 더 나오면 `Confirm merge`를 클릭합니다.
4. 머지가 완료되면 초안 파일이 `main` 브랜치에 반영됩니다.

머지 후에는 로컬 컴퓨터의 VS Code에서도 최신 파일을 받아와야 합니다.

### 1-5. VS Code에서 동기화

1. VS Code를 엽니다.
2. `dafnelee89/info-blog` 폴더를 엽니다.
3. 왼쪽 Source Control 아이콘을 클릭합니다.
4. 상단의 점 세 개 아이콘을 클릭합니다.
5. 그래프 옆의 `Pull` 또는 `Sync Changes` 버튼을 클릭합니다.

동기화가 끝나면 `_drafts` 폴더 안에 새 초안 파일이 들어와 있어야 합니다.

## 2. 블로그 초안을 포스팅으로 전환하기

### 2-1. 초안 파일 위치 확인

초안 파일은 보통 아래 폴더에 생성됩니다.

```text
_drafts
```

예시는 아래와 같습니다.

```text
_drafts/2026-07-20-apostille-student-document-issuance-translation.md
```

파일명은 보통 아래 형식입니다.

```text
YYYY-MM-DD-영문-slug.md
```

여기서 `slug`는 글 주소에 들어가는 영문 이름입니다.

### 2-2. `_drafts`에서 `_posts`로 이동

포스팅으로 공개하려면 초안 파일을 `_posts` 폴더로 옮깁니다.

이동 전:

```text
_drafts/2026-07-20-example-post.md
```

이동 후:

```text
_posts/2026-07-20-example-post.md
```

주의할 점은 다음과 같습니다.

- 파일명 앞 날짜가 실제 발행일과 맞는지 확인합니다.
- 날짜 형식은 `YYYY-MM-DD`로 유지합니다.
- 파일 확장자는 `.md`로 유지합니다.
- 파일명에 한글이나 공백을 넣지 않습니다.

### 2-3. Front Matter 확인

글 파일 맨 위에는 아래처럼 `---`로 감싼 설정 영역이 있습니다. 이 부분을 Front Matter라고 합니다.

```yaml
---
title: "글 제목"
description: "검색 결과나 미리보기에서 보일 설명"
slug: "example-post"
date: 2026-07-20 09:00:00 +0900
current_as_of: 2026-07-20
update_cycle: monthly
source_type: official
categories: ["How-To and Requirements", "Work and Life Admin"]
tags: ["태그1", "태그2", "2026"]
image:
  path: /assets/img/posts/blog/example-post/preview.png
  alt: "대표 이미지 설명"
---
```

특히 아래 항목을 확인합니다.

- `title`: 제목이 너무 길거나 어색하지 않은지 확인
- `description`: 글 내용을 한 문장으로 잘 설명하는지 확인
- `slug`: 파일명 뒤쪽 영문 slug와 같은지 확인
- `date`: 발행 날짜와 시간이 맞는지 확인
- `current_as_of`: 글 내용의 기준 날짜가 맞는지 확인
- `categories`: 기존 글과 비슷한 카테고리를 사용했는지 확인
- `tags`: 검색에 도움 되는 태그가 들어갔는지 확인
- `image.path`: 대표 이미지 파일 경로가 실제 파일 위치와 맞는지 확인
- `image.alt`: 이미지 내용을 설명하는 문장인지 확인

## 3. 글 내용 다듬기

초안은 자동으로 만들어졌기 때문에 그대로 올리기보다 한 번 읽고 수정하는 것이 좋습니다.

### 3-1. 먼저 전체 흐름 확인

글을 처음부터 끝까지 읽으면서 아래를 확인합니다.

- 제목과 본문 내용이 서로 맞는지
- 첫 문단에서 결론이 바로 보이는지
- 중간 제목이 자연스럽게 이어지는지
- 같은 말이 반복되지 않는지
- 문장이 너무 딱딱하거나 번역체처럼 보이지 않는지
- 표나 목록이 실제로 도움이 되는지
- 마지막 정리가 글 전체와 잘 맞는지

### 3-2. 오타와 어색한 표현 수정

수정할 때는 아래 기준으로 보면 좋습니다.

- `확인해보니` 같은 표현이 너무 많이 반복되면 줄이기
- `현재 기준으로는`이 필요한 곳에만 남기기
- 너무 긴 문장은 두 문장으로 나누기
- 독자가 바로 행동할 수 있게 구체적인 동사를 쓰기
- 출처 확인이 필요한 내용은 단정적으로 쓰지 않기

예시는 아래와 같습니다.

수정 전:

```text
환불을 받을 수 있습니다.
```

수정 후:

```text
판매자 정책과 결제수단에 따라 환불 가능 여부가 달라질 수 있습니다.
```

### 3-3. 개인 의견 영역 채우기

글 하단에 `나의 의견 1`, `나의 의견 2` 같은 영역이 있으면 그대로 두지 말고 실제 문장으로 바꿉니다.
적을 내용이 없거나 생략하고 싶으면 지워도 됩니다.

예시는 아래와 같습니다.

```text
## 나의 의견 1

실제로 이런 문제를 겪을 때 가장 먼저 해야 할 일은 판매자에게 감정적으로 항의하는 것이 아니라, 송장 상태와 통관 보류 사유를 캡처해 두는 것입니다. 증빙이 있어야 플랫폼 분쟁이나 카드사 이의제기 단계에서도 설명이 쉬워집니다.
```

## 4. 이미지 만들기

글 하나당 이미지는 기본적으로 2개를 준비합니다.

- 대표 이미지: `preview.png`
- 본문 이미지: `image-1.webp`, `image-2.webp`

이미지 개수는 글에 따라 달라질 수 있지만, 현재 작업 기준은 글당 본문 이미지 2개입니다.

### 4-1. 이미지 프롬프트 만들기

본문에 들어갈 장면을 먼저 정합니다.

예시는 아래와 같습니다.

```text
통관 지연 상황, 송장과 통관서류를 확인하는 모습 일러스트
```

이미지 설명은 아래 사이트에서 생성합니다.

[Gemini 이미지 생성](https://gemini.google.com/app?android-min-version=301356232&ios-min-version=322.0&is_sa=1&hl=ko-KR&utm_campaign=microsite_gemini_image_generation_page&icid=microsite_gemini_image_generation_page&utm_source=gemini&utm_medium=web)

프롬프트를 만들 때는 아래 요소를 넣으면 결과가 더 안정적입니다.

- 글 주제와 관련된 실제 상황
- 인물 또는 사물
- 배경
- 분위기
- 일러스트인지 사진풍인지
- 글에 어울리는 색감

예시는 아래와 같습니다.

```text
해외직구 통관 지연 상황을 설명하는 블로그용 일러스트. 책상 위에 노트북, 송장 번호, 통관 서류가 놓여 있고 사용자가 배송 조회 화면을 확인하는 장면. 깔끔하고 신뢰감 있는 정보성 블로그 스타일.
```

### 4-2. 이미지 다운로드

이미지 생성이 끝나면 글에 사용할 이미지 2개를 다운로드합니다.

다운로드 후에는 파일을 바로 옮기기 전에 아래를 확인합니다.

- 이미지가 글 내용과 맞는지
- 글자나 로고가 이상하게 생성되지 않았는지
- 너무 어둡거나 복잡하지 않은지
- 썸네일로 작게 보아도 의미가 전달되는지
- 사람 손, 문서, 화면 같은 디테일이 크게 깨지지 않았는지

### 4-3. WebP로 변환

다운로드한 이미지는 아래 사이트에서 WebP로 변환합니다.

[Shutterstock PNG to WebP 변환기](https://www.shutterstock.com/ko/image-converter/png-to-webp)

변환 순서는 다음과 같습니다.

1. 사이트에 접속합니다.
2. 다운로드한 PNG 또는 JPG 이미지를 업로드합니다.
3. 변환이 끝날 때까지 기다립니다.
4. WebP 파일을 다운로드합니다.
5. 파일명을 `image-1.webp`, `image-2.webp`처럼 변경합니다.

## 5. 이미지 파일 경로 정리

이미지는 글의 `slug`와 같은 이름의 폴더에 넣습니다.

예를 들어 글의 `slug`가 아래와 같다면,

```text
overseas-purchase-clearance-delay-refund-return-procedure
```

이미지 폴더는 아래처럼 만듭니다.

```text
assets/img/posts/blog/overseas-purchase-clearance-delay-refund-return-procedure
```

파일 구조는 아래처럼 맞춥니다.

```text
assets/img/posts/blog/overseas-purchase-clearance-delay-refund-return-procedure/preview.png
assets/img/posts/blog/overseas-purchase-clearance-delay-refund-return-procedure/image-1.webp
assets/img/posts/blog/overseas-purchase-clearance-delay-refund-return-procedure/image-2.webp
```

본문에서 이미지를 넣을 때는 앞에 `/`를 붙여서 아래처럼 작성합니다.

```markdown
![통관 지연 상황, 송장과 통관서류를 확인하는 모습](/assets/img/posts/blog/overseas-purchase-clearance-delay-refund-return-procedure/image-1.webp)
```

대표 이미지는 Front Matter의 `image.path`에서 아래처럼 연결합니다.

```yaml
image:
  path: /assets/img/posts/blog/overseas-purchase-clearance-delay-refund-return-procedure/preview.png
  alt: "해외직구 통관지연 대응 썸네일"
```

## 6. 로컬 환경에서 확인

### 6-1. 프로젝트 폴더로 이동

터미널에서 아래 명령어를 실행합니다.

### 6-2. 패키지 설치

처음 실행하거나 패키지가 바뀐 경우 아래 명령어를 실행합니다.

```bash
npm install
```

이미 설치되어 있다면 매번 실행하지 않아도 됩니다.

### 6-3. 로컬 서버 실행

아래 명령어를 실행합니다.

```bash
npm run dev
```

서버가 정상적으로 실행되면 브라우저에서 아래 주소로 접속합니다.

```text
http://localhost:4101
```

또는 환경에 따라 아래 주소로 접속합니다.

```text
http://127.0.0.1:4101
```

### 6-4. 화면에서 확인할 것

브라우저에서 아래 항목을 확인합니다.

- 글이 목록에 보이는지
- 글 제목이 깨지지 않는지
- 대표 이미지가 보이는지
- 본문 이미지가 모두 보이는지
- 이미지가 너무 크거나 작지 않은지
- 표가 모바일 화면에서 보기 어렵지 않은지
- 링크를 클릭했을 때 정상 페이지로 이동하는지
- `나의 의견` 같은 임시 문구가 남아 있지 않은지
- 맞춤법, 띄어쓰기, 문장 흐름이 자연스러운지

## 7. 자주 생기는 문제와 해결 방법

### 글이 목록에 안 보일 때

아래를 확인합니다.

- 파일이 `_posts` 폴더에 있는지
- 파일명 앞 날짜가 `YYYY-MM-DD` 형식인지
- Front Matter의 `date`가 미래 날짜로 되어 있지 않은지
- 파일 맨 위와 맨 아래의 `---`가 빠지지 않았는지
- 로컬 서버를 껐다가 다시 실행했는지

### 이미지가 안 보일 때

아래를 확인합니다.

- 이미지 파일이 실제로 `assets/img/posts/blog/slug/` 폴더에 있는지
- 본문 이미지 경로가 `/assets/img/posts/blog/slug/image-1.webp` 형식인지
- 파일명 대소문자가 정확한지
- 확장자가 `.webp`인지 `.png`인지 정확히 맞는지
- Front Matter의 `image.path`가 실제 대표 이미지 경로와 같은지

### 로컬 서버가 실행되지 않을 때

아래 순서로 확인합니다.

1. 현재 위치가 `dafnelee89/info-blog` 폴더인지 확인합니다.
2. `npm install`을 다시 실행합니다.
3. `npm run dev`를 다시 실행합니다.
4. 이미 4101 포트를 쓰는 서버가 있으면 기존 터미널을 종료하고 다시 실행합니다.

현재 위치 확인 명령어는 아래와 같습니다.

```bash
pwd
```

## 8. 최종 업로드 전 체크리스트

GitHub에 올리기 전 아래를 확인합니다.

- `_drafts`에 공개할 글이 남아 있지 않은지 확인
- 새 글이 `_posts`에 들어갔는지 확인
- 파일명 날짜와 Front Matter의 `date`가 맞는지 확인
- `slug`와 이미지 폴더명이 맞는지 확인
- 대표 이미지 `preview.png`가 있는지 확인
- 본문 이미지 `image-1.webp`, `image-2.webp`가 있는지 확인
- 본문 이미지 링크가 깨지지 않는지 확인
- 임시 문구가 남아 있지 않은지 확인
- 로컬 서버에서 글 화면을 직접 확인

## 9. GitHub에 반영하기

수정이 끝나면 VS Code Source Control에서 변경 파일을 확인합니다.

터미널로 진행할 경우 예시는 아래와 같습니다.

```bash
git status
git add _posts assets/img/posts
git commit -m "Add new info blog post"
git push
```

## 10. 빠른 요약

급할 때는 아래 순서만 기억하면 됩니다.

1. `blog-automation`에서 `Generate Topic Candidates` 실행
2. 이슈 댓글에 `pick 번호` 입력
3. `dafnelee89/info-blog`에 생성된 Pull Request 머지
4. VS Code에서 Pull 또는 Sync
5. `_drafts` 파일을 `_posts`로 이동
6. 글 내용과 Front Matter 수정
7. 이미지 2개 생성 후 WebP 변환
8. `assets/img/posts/blog/slug/` 폴더에 이미지 저장
9. `npm run dev`로 로컬 확인
10. 이상 없으면 커밋하고 푸시
