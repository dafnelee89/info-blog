# 로컬 개발환경

이 문서는 `dafnelee89/info-blog`를 맥북 Neo 로컬 환경에서 실행하기 위한 절차를 정리한 문서입니다.

## 대상 환경

- 기기: 맥북 Neo
- 운영체제: macOS
- 프로젝트 경로: `/Users/henjini/github-blog-project/dafnelee89/info-blog`
- 사이트 엔진: Jekyll
- 테마: `jekyll-theme-chirpy`
- 로컬 서버 주소: `http://127.0.0.1:4101/`

현재 확인된 실행 버전은 다음과 같습니다.

```bash
node -v
# v26.0.0

npm -v
# 11.12.1

rbenv exec ruby -v
# ruby 3.3.6

rbenv exec bundle -v
# Bundler 4.0.16
```

## 전체 설치 순서

Ruby, gem, Bundler, Node.js가 전혀 설치되어 있지 않은 새 맥북 Neo라면 아래 순서대로 진행합니다.

1. Terminal 앱 열기
2. Xcode Command Line Tools 설치
3. Homebrew 설치
4. rbenv와 ruby-build 설치
5. Ruby 3.3.6 설치
6. Bundler 4.0.16 설치
7. Node.js 설치
8. 프로젝트 폴더로 이동
9. Node 패키지와 Ruby gem 설치
10. 빌드 확인
11. 로컬 서버 실행

## 터미널 열기

1. macOS에서 `Terminal` 앱을 엽니다.
2. 아래 명령어로 현재 셸이 `zsh`인지 확인합니다. macOS 기본 터미널은 보통 `zsh`입니다.

```bash
echo $SHELL
```

아래처럼 `/bin/zsh`가 포함되어 있으면 정상입니다.

```text
/bin/zsh
```

## Xcode Command Line Tools 설치

Ruby를 직접 빌드하려면 macOS 컴파일 도구가 필요합니다. 새 맥북에서 가장 먼저 설치합니다.

```bash
xcode-select --install
```

설치 팝업이 뜨면 `Install`을 누르고 완료될 때까지 기다립니다.

설치가 끝났는지 확인합니다.

```bash
xcode-select -p
```

아래처럼 출력되면 정상입니다.

```text
/Library/Developer/CommandLineTools
```

## Homebrew 설치

Homebrew는 macOS에서 개발 도구를 설치하는 패키지 매니저입니다.

아래 명령어를 터미널에 입력합니다.

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

설치 중 비밀번호를 물어보면 맥북 로그인 비밀번호를 입력합니다. 입력해도 화면에는 글자가 보이지 않는 것이 정상입니다.

Apple Silicon 맥북 Neo에서는 Homebrew가 보통 `/opt/homebrew`에 설치됩니다. 설치 후 터미널에 안내되는 `eval` 명령을 실행해야 `brew` 명령을 바로 사용할 수 있습니다.

아래 명령어를 입력합니다.

```bash
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"
```

Homebrew가 정상 설치되었는지 확인합니다.

```bash
brew --version
brew doctor
```

`brew doctor`에서 `Your system is ready to brew.`가 나오면 가장 좋습니다. 경고가 나오더라도 설치 자체가 실패한 것은 아닐 수 있습니다.

## rbenv와 Ruby 설치

이 프로젝트는 `.ruby-version`에 Ruby `3.3.6`을 사용하도록 지정되어 있습니다. macOS 기본 Ruby를 사용하지 않고 `rbenv`로 프로젝트 Ruby를 설치합니다.

### 1. rbenv 설치

```bash
brew install rbenv ruby-build
```

설치 후 `zsh` 설정 파일에 rbenv 초기화 코드를 추가합니다.

```bash
echo 'eval "$(rbenv init - zsh)"' >> ~/.zshrc
source ~/.zshrc
```

rbenv가 정상 설치되었는지 확인합니다.

```bash
rbenv --version
```

### 2. Ruby 3.3.6 설치

```bash
rbenv install 3.3.6
```

설치에는 시간이 걸릴 수 있습니다. 컴파일 중 터미널을 닫지 않습니다.

설치 후 전역 기본 Ruby를 3.3.6으로 지정합니다.

```bash
rbenv global 3.3.6
rbenv rehash
```

Ruby 버전을 확인합니다.

```bash
ruby -v
```

아래처럼 `3.3.6`이 포함되어 있으면 정상입니다.

```text
ruby 3.3.6
```

프로젝트 폴더 안에서는 더 안전하게 아래처럼 확인합니다.

```bash
rbenv exec ruby -v
```

## gem과 Bundler 설치

`gem`은 Ruby 패키지 관리자입니다. Ruby를 설치하면 보통 함께 설치됩니다.

```bash
gem -v
```

이 프로젝트의 `Gemfile.lock`은 Bundler `4.0.16`을 기준으로 합니다. 아래 명령어로 Bundler를 설치합니다.

```bash
gem install bundler:4.0.16
rbenv rehash
```

설치 확인:

```bash
bundle -v
rbenv exec bundle -v
```

아래처럼 나오면 정상입니다.

```text
Bundler version 4.0.16
```

## Node.js와 npm 설치

Node.js는 npm 패키지 설치와 CSS 빌드에 사용합니다.

Homebrew로 Node.js를 설치합니다.

```bash
brew install node
```

설치 확인:

```bash
node -v
npm -v
```

현재 이 프로젝트에서 확인한 버전은 다음과 같습니다.

```text
node v26.0.0
npm 11.12.1
```

버전 숫자가 완전히 같지 않아도 보통 실행에는 문제가 없습니다. 다만 너무 오래된 Node.js를 쓰면 `npm ci` 또는 `purgecss` 실행에서 문제가 날 수 있으니 Homebrew로 설치한 최신 안정 버전을 권장합니다.

## 프로젝트 폴더로 이동

프로젝트가 이미 맥북에 받아져 있다면 아래 명령어로 이동합니다.

```bash
cd /Users/henjini/github-blog-project/dafnelee89/info-blog
```

현재 위치가 맞는지 확인합니다.

```bash
pwd
```

아래처럼 출력되면 정상입니다.

```text
/Users/henjini/github-blog-project/dafnelee89/info-blog
```

프로젝트 폴더가 없다면 먼저 저장소를 받은 뒤 이동해야 합니다. 저장소를 받을 상위 폴더로 이동한 다음 `git clone`을 실행합니다.

```bash
mkdir -p /Users/henjini/github-blog-project/dafnelee89
cd /Users/henjini/github-blog-project/dafnelee89
git clone https://github.com/dafnelee89/info-blog.git
cd info-blog
```

## 필수 도구

이 프로젝트는 Node.js와 Ruby를 함께 사용합니다.

- Node.js: npm 패키지 설치와 CSS 빌드에 사용
- npm: `package.json` 스크립트 실행에 사용
- rbenv: 프로젝트 Ruby 버전 고정에 사용
- Ruby 3.3.6: `.ruby-version`에 명시된 Jekyll 실행 버전
- Bundler 4.0.16: `Gemfile.lock`에 명시된 Ruby gem 설치 버전

설치 여부는 아래 명령어로 확인합니다.

```bash
node -v
npm -v
rbenv versions
rbenv exec ruby -v
rbenv exec bundle -v
```

`rbenv versions`에서 `3.3.6` 앞에 `*` 표시가 있거나, 현재 프로젝트 경로의 `.ruby-version`이 `3.3.6`을 가리키면 정상입니다.

## 프로젝트 의존성 설치

Ruby, Bundler, Node.js 설치가 끝난 뒤 프로젝트 안에서 Node 패키지와 Ruby gem을 설치합니다.

### 1. Node 패키지 설치

```bash
npm ci
```

이 명령어는 `package-lock.json` 기준으로 정확한 Node 의존성을 설치합니다. 설치가 끝나면 `node_modules` 폴더가 생성됩니다.

### 2. Ruby gem 설치

```bash
rbenv exec bundle install
```

이 명령어는 `Gemfile.lock` 기준으로 Jekyll, Chirpy 테마, Jekyll 플러그인 등을 설치합니다.

설치가 정상인지 확인하려면 아래 명령어를 실행합니다.

```bash
rbenv exec bundle check
```

정상이라면 모든 gem이 설치되어 있다는 메시지가 출력됩니다.

## 로컬 서버 실행

프로젝트 폴더에서 아래 명령어를 실행합니다.

```bash
npm run dev
```

이 명령어는 내부적으로 다음 Jekyll 서버 명령을 실행합니다.

```bash
rbenv exec bundle exec jekyll serve --host 127.0.0.1 --port 4101 --force_polling
```

터미널에 아래와 비슷한 메시지가 나오면 서버가 실행된 상태입니다.

```text
Server address: http://127.0.0.1:4101/
Server running... press ctrl-c to stop.
```

브라우저에서 아래 주소를 엽니다.

```text
http://127.0.0.1:4101/
```

## 서버 중지

서버를 실행 중인 터미널에서 `Control + C`를 누릅니다.

터미널 프롬프트가 다시 나타나면 서버가 중지된 것입니다.

## 정적 빌드

GitHub Pages에 올리기 전처럼 정적 사이트가 정상 생성되는지 확인하려면 아래 명령어를 실행합니다.

```bash
npm run build
```

이 명령어는 내부적으로 다음 순서로 실행됩니다.

```bash
npm run build:css
rbenv exec bundle exec jekyll build
```

빌드가 성공하면 `_site` 폴더에 정적 HTML 결과물이 생성됩니다.

## 빌드 결과 정리

Jekyll 빌드 결과와 캐시를 정리하려면 아래 명령어를 실행합니다.

```bash
npm run clean
```

필요하면 Node 패키지를 다시 깨끗하게 설치할 수 있습니다.

```bash
npm ci
```

## 자주 쓰는 명령어

개발 도구가 이미 설치된 뒤에는 아래 명령어를 자주 사용합니다.

```bash
# 프로젝트 폴더 이동
cd /Users/henjini/github-blog-project/dafnelee89/info-blog

# Node 의존성 설치
npm ci

# Ruby gem 설치
rbenv exec bundle install

# 로컬 개발 서버 실행
npm run dev

# 정적 빌드
npm run build

# Jekyll 빌드 결과 정리
npm run clean

# 인덱싱 준비 상태 확인
npm run verify:indexing
```

## 문제 해결

### bundle 명령이 macOS 기본 Ruby를 사용하는 경우

아래처럼 `/usr/bin/ruby` 또는 Ruby 2.6이 잡히면 Jekyll 의존성이 맞지 않을 수 있습니다.

```bash
ruby -v
which ruby
```

이 프로젝트에서는 `rbenv exec`를 붙여 실행합니다.

```bash
rbenv exec ruby -v
rbenv exec bundle install
npm run dev
```

### Could not find bundler 오류

`Gemfile.lock`은 Bundler `4.0.16`을 사용합니다. 아래 명령어로 현재 Bundler를 확인합니다.

```bash
rbenv exec bundle -v
```

Bundler가 없거나 버전이 맞지 않으면 아래 명령어를 실행합니다.

```bash
rbenv exec gem install bundler:4.0.16
rbenv rehash
```

그 다음 다시 설치합니다.

```bash
rbenv exec bundle install
```

### purgecss: command not found

Node 패키지가 설치되지 않은 상태입니다.

```bash
npm ci
```

설치 후 다시 빌드합니다.

```bash
npm run build
```

### 4101 포트가 이미 사용 중인 경우

기존에 실행 중인 Jekyll 서버가 있을 수 있습니다. 실행 중인 서버 터미널에서 `Control + C`를 누른 뒤 다시 실행합니다.

```bash
npm run dev
```

다른 포트를 임시로 쓰고 싶다면 아래처럼 직접 실행할 수 있습니다.

```bash
rbenv exec bundle exec jekyll serve --host 127.0.0.1 --port 4102 --force_polling
```

이 경우 브라우저 주소는 다음과 같습니다.

```text
http://127.0.0.1:4102/
```

### /Users/henjini is not writable 메시지

샌드박스나 제한된 실행 환경에서 Bundler가 홈 디렉터리에 쓰지 못할 때 나올 수 있는 경고입니다. 로컬 터미널에서 정상적으로 빌드와 서버 실행이 된다면 무시해도 됩니다.

### rbenv install 3.3.6이 실패하는 경우

대부분 Xcode Command Line Tools 또는 Homebrew 빌드 의존성이 부족할 때 발생합니다.

먼저 Command Line Tools 설치 상태를 확인합니다.

```bash
xcode-select -p
```

설치되어 있지 않다면 다시 설치합니다.

```bash
xcode-select --install
```

Homebrew 상태도 확인합니다.

```bash
brew doctor
brew update
```

그 다음 Ruby 설치를 다시 시도합니다.

```bash
rbenv install 3.3.6
rbenv global 3.3.6
rbenv rehash
```

## 권장 작업 순서

### 새 맥북 Neo에서 처음 준비할 때

Ruby, gem, Bundler, Node.js가 아무것도 없다고 가정하면 아래 순서대로 진행합니다.

```bash
# 1. Xcode Command Line Tools 설치
xcode-select --install

# 2. Homebrew 설치
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 3. Homebrew PATH 설정
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"

# 4. rbenv, ruby-build 설치
brew install rbenv ruby-build

# 5. rbenv 셸 설정
echo 'eval "$(rbenv init - zsh)"' >> ~/.zshrc
source ~/.zshrc

# 6. Ruby 3.3.6 설치
rbenv install 3.3.6
rbenv global 3.3.6
rbenv rehash

# 7. Bundler 설치
gem install bundler:4.0.16
rbenv rehash

# 8. Node.js 설치
brew install node

# 9. 프로젝트 폴더 이동
cd /Users/henjini/github-blog-project/dafnelee89/info-blog

# 10. 프로젝트 의존성 설치
npm ci
rbenv exec bundle install

# 11. 빌드 확인
npm run build

# 12. 로컬 서버 실행
npm run dev
```

### 개발 도구 설치 후 처음 실행할 때

이미 Homebrew, rbenv, Ruby, Bundler, Node.js가 설치되어 있다면 아래 순서대로 입력합니다.

```bash
cd /Users/henjini/github-blog-project/dafnelee89/info-blog
npm ci
rbenv exec bundle install
npm run build
npm run dev
```

평소 글을 작성하거나 디자인을 확인할 때는 아래 두 줄만 사용하면 됩니다.

```bash
cd /Users/henjini/github-blog-project/dafnelee89/info-blog
npm run dev
```

## 참고 링크

- Apple Command Line Tools: https://developer.apple.com/documentation/xcode/installing-the-command-line-tools/
- Homebrew: https://brew.sh/
- rbenv: https://github.com/rbenv/rbenv
- ruby-build: https://github.com/rbenv/ruby-build
- Jekyll: https://jekyllrb.com/
